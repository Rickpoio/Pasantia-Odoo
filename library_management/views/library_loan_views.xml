<odoo>
    <record id="view_library_loan_list" model="ir.ui.view">
        <field name="name">library.loan.list</field>
        <field name="model">library.loan</field>
        <field name="arch" type="xml">
            <list decoration-info="state == 'ongoing'" decoration-success="state == 'returned'">
                <field name="book_id"/>
                <field name="partner_id"/>
                <field name="loan_date"/>
                <field name="state" widget="badge"/>
                <button name="action_return_book" type="object" string="Devolver" 
                        invisible="state == 'returned'" class="oe_highlight"/>
            </list>
        </field>
    </record>

    <record id="action_library_loan" model="ir.actions.act_window">
        <field name="name">Préstamos</field>
        <field name="res_model">library.loan</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_library_loans" name="Préstamos" parent="menu_library_catalog" action="action_library_loan" sequence="20"/>
</odoo>